/** `/sign-in` route — renders the shared staff sign-in screen. */

import { SignInScreen } from "@/components/auth/SignInScreen";

/** Route entry for staff authentication. */
export default function SignInRoute() {
  return <SignInScreen />;
}
